package com.escola.achadosperdidos.ui.theme

import androidx.compose.ui.graphics.Color

// ── Paleta da escola (estilo "bolha") ───────────────────────────────────────

/** Azul Celeste — fundo da tela. */
val AzulCelesteClaro  = Color(0xFFB3E5FC)
/** Azul Celeste — top bar e elementos primários. */
val AzulCeleste       = Color(0xFF4FC3F7)
/** Azul Celeste mais escuro — botões e estados pressionados. */
val AzulCelesteEscuro = Color(0xFF0288D1)

/** Branco — cards. */
val Branco            = Color(0xFFFFFFFF)
val BrancoQuente      = Color(0xFFFAFAFA)

/** Texto principal. */
val CinzaTexto        = Color(0xFF263238)
val CinzaSecundario   = Color(0xFF607D8B)
val CinzaClaro        = Color(0xFFECEFF1)

/** Vermelho — destaque para itens não devolvidos / erros. */
val VermelhoAlerta    = Color(0xFFE53935)
val VermelhoClaro     = Color(0xFFFFCDD2)

/** Verde — sucesso (item devolvido, sync OK). */
val VerdeSucesso      = Color(0xFF66BB6A)
